# MacodeAstrologyAPI — Environment Variables
# Copy to .env and fill in your values.

# Basic Auth credentials (same format as the old AstrologyAPI)
API_USER_ID=alex
API_KEY=your-secret-key-here

# Set to "true" only in local dev to bypass auth
SKIP_AUTH=false
